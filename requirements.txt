# Runtime dependencies for Vercel (@vercel/python installs these).
# uvicorn is NOT needed — Vercel provides the ASGI server.
# google-ads is intentionally excluded (heavy optional extra).
# (Render uses uv + pyproject instead — see render.yaml.)
fastapi>=0.139.2
pydantic-settings>=2.14.2
pyyaml>=6.0.3
anthropic>=0.118.0
